import React from 'react';
import { useParams } from 'react-router';

const SingleFile = () => {
    const { singleFileId } = useParams();
    return (
        <div>
            <h2>hhhhhhhh</h2>
        </div>
    );
};

export default SingleFile;